mode: train
env:
  device: cuda
  device_ids: []
  checkpoint_path : ./
  train_path : data/
  test_path :
  validation_path :
  output_path : output/
hp: 
  lr : 0.001
  ld : 0.95
  batch_size : 500
  epochs : 100